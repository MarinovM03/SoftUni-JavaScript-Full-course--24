function solve(input) {
    let a = 5;
    let b = "5";

    console.log(a === b);
}

solve();

function solve(input){
    let a = "Martin";
    let b = "Martin";

    console.log(a === b);

}

solve();


// == - compares by value.
// === - compares by value and type.
// != - looks for difference in value (if different will output TRUE)
// !== - looks for difference in value and type (if different will output TRUE)
// < - check if smaller
// > - checks if bigger
// <= - checks if smaller and equal
// >= - checks if bigger and equal